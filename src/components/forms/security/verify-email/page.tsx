import { NewEmailForm } from '../_components/NewEmailForm';
import { FC } from 'react';

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <NewEmailForm />;
};

export default page;
