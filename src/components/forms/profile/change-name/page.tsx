import { NameEditForm } from '../_components/NameEditForm';
import { FC } from 'react';

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <NameEditForm />;
};

export default page;
