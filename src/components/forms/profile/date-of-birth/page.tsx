import { DateOfBirthForm } from '../_components/DateOfBirthForm';
import { FC } from 'react';

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <DateOfBirthForm />;
};

export default page;
