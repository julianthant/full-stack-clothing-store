import { FC } from 'react';
import { PaymentEditForm } from '../_components/PaymentEditForm';

const page: FC = () => {
  return (
    <div>
      <PaymentEditForm />
    </div>
  );
};

export default page;
