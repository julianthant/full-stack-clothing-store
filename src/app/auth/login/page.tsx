import { LoginForm } from '@/app/auth/_components/LoginForm';
import { FC } from 'react';

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <LoginForm />;
};

export default page;
