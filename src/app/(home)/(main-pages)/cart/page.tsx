import { Metadata } from 'next';

interface cartProps {}

export const metadata: Metadata = {
  title: 'Cart | Clothes.co',
};

const cart = () => {
  return <div>Cart</div>;
};

export default cart;
